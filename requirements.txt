# Core Django
Django==4.2.5
djangorestframework==3.14.0
django-cors-headers==4.2.0

## Non django dependencies
# WebSocket Support
channels==4.0.0
channels-redis==4.1.0
daphne==4.0.0

# Database
psycopg2-binary==2.9.10
redis==5.0.0

# Authentication & Security
djangorestframework-simplejwt==5.5.1
python-jose[cryptography]==3.3.0
python-dotenv==1.0.0
django-environ==0.11.2
firebase-admin==6.5.0

# Async Task Queue
celery==5.3.1

# AI & Audio Processing
requests==2.31.0
aiohttp==3.9.5
google-generativeai==0.7.2
google-genai>=1.7.0
faster-whisper>=1.0.0,<2
speechbrain==1.0.1
openai-whisper==20231117; python_version < "3.13"
gTTS==2.5.0

# Optional heavy extras (install only if needed; require CUDA-capable environment or large downloads)
# pyannote-audio==3.1.1  # for high-quality VAD/diarization (requires torch + HF token)
# whisperx==3.1.1        # for Whisper + alignment
# montreal-forced-aligner==2.2.17  # MFA; usually installed via conda/installer, not pip

# NOTE: Do not install the deprecated 'google' meta-package as it conflicts with namespace packages
# If you accidentally installed it, run: pip uninstall -y google
# NOTE: SpeechBrain depends on torch/torchaudio, which pip will resolve automatically.
# Ensure ffmpeg is installed on the server for audio format conversion.

# API Documentation
drf-spectacular==0.26.4

# Development Tools
django-extensions==3.2.3
ipython==8.15.0

# Production Server
gunicorn==21.2.0
whitenoise==6.5.0

# Testing (do not install coverage in production to avoid import-time type mismatches)
pytest==7.4.2
pytest-django==4.5.2
pytest-asyncio==0.21.1
factory-boy==3.3.0

# Monitoring & Logging
django-debug-toolbar==4.2.0
sentry-sdk==1.32.0
django-storages==1.14.2
boto3==1.34.162

# Utilities
Pillow==10.0.1
python-dateutil==2.8.2
